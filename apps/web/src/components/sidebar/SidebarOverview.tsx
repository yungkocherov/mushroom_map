/**
 * Overview sidebar — eyebrow + H1 + DateScrubber + список «топ-5
 * районов сегодня» + источники + preview-badge. Phase 1: скелет.
 */
export interface SidebarOverviewProps {
  className?: string;
}

export function SidebarOverview(_props: SidebarOverviewProps) {
  return <div data-todo="phase-2" />;
}
