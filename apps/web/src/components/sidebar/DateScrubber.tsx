/**
 * DateScrubber — горизонтальный ряд из 7 «пилюль» (по дням), при
 * раскрытии — 14 дней. Подписан на `useForecastDate`. Phase 1: скелет.
 */
export interface DateScrubberProps {
  className?: string;
}

export function DateScrubber(_props: DateScrubberProps) {
  return <div data-todo="phase-2" />;
}
