# @mushroom-map/tokens

Design tokens — colours, typography scale, spacing, radius, shadow — as both CSS custom properties (for web) and typed TypeScript constants (for future mobile / RN).

The tokens live here rather than in `apps/web` so the eventual Expo app can consume the same values via the TS export without duplicating the palette.

Populated in Phase 0 commit (c).
