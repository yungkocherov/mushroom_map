# @mushroom-map/types

Typed representations of the API payloads: `ForestAtResponse`, `SpeciesRef`, `SoilPolygon`, `ForecastPrediction`, etc.

Lives outside `apps/web` so future clients (mobile app, CLI tools, test fixtures) can share the same type surface with the web app.

Populated in Phase 0 commit (d).
